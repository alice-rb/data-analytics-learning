# SQL Exercises

This folder contains practical SQL exercises, including:

- Basic queries (`SELECT`, `WHERE`, `ORDER BY`)
- Joins and subqueries
- Aggregate functions and groupings (`GROUP BY`, `HAVING`)
- Simple procedures and views

